---
layout: false
---
<%
	entries = [] 
	blog.articles.each do |article|
		entry = {
			:title => article.title, 
			:url => article.url,
			:content => article.body,
			:summary => article.first_paragraph
		}
		entries << entry
	end
%><%=entries.to_json %>
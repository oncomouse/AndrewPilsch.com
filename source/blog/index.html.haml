---
layout: blog
pageable: false
per_page: 6
---
%h1.role Recent Articles
- page_articles.each_with_index do |article, i|
	%article
		%h2
			= link_to article.title, article
			%span= article.date.strftime('%b %e, %Y')
		.article_body
			= article.first_paragraph
			%p 
				= link_to("Continue Reading", article.url)

- if page_articles.length == 0
	
	%h1.article_title Nothing Posted Yet

- if page_articles.length > 0 and articles.length > page_articles.length
	%article
		%p.archives
			=link_to "View the Archives", "archives.html"
<% 
require "uri"
require "open-uri"
current_term = "#{CourseDate.to_term(Date.today.month)} #{Date.today.year}"

course_html = build? ? URI.parse("http://andrew.pilsch.com/courses?blank&term=#{URI.encode(current_term)}").read.to_s.force_encoding("UTF-8") : "" %>
<%= course_html %>
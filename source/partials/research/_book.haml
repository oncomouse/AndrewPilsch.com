- require 'base64'
.col2.expand.box.research.article.no-excerpt{ :id => "#{book["id"]}" }
	.hideable
		%h2= book["published"] ? "Published Book" : "Book In Progress"
		%p
			=inline_image book["image"], :alt => "Image Illustrating the book, \"#{book["short_title"]}\""
		-presentation_title = capture_haml do
			= markdown do
				#{book["short_title"]}
		%h1= presentation_title.gsub(/\<\/{0,1}[Pp]\>/,"") 
		- if book["short_description"]
			= markdown do
				#{book["short_description"]}
	.expandable
		.mobile
			%h2= book["published"] ? "Published Book" : "Book In Progress" 
		-presentation_title = capture_haml do
			= markdown do
				#{book["title"]}
		%h1= presentation_title.gsub(/\<\/{0,1}[Pp]\>/,"")
		= markdown do
			#{book["description"]}